# Generic helpers used across multiple features (not tied to one domain).

def add_strings(a: str, b: str) -> str:
    """Example generic utility: concatenate two strings with a space."""
    return f"{a} {b}".strip()
